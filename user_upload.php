#!/usr/bin/env php
<?php
echo "~~ Initialising user upload ~~ \n";
$commandOptions = ["file:"];
$commandValues = getopt('file:', $commandOptions);

// Read input file, validate command options

// Initialise DB connection, create DB

// Iterate over file contents validating and prepping for insert

?>
